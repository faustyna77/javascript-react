// vite.config.js
import { defineConfig } from 'vite';
import dotenv from 'dotenv';

// Wczytaj zmienne środowiskowe z pliku .env
dotenv.config();

export default defineConfig({
  // Konfiguracje Vite
  // Tutaj możesz dodać różne opcje konfiguracyjne Vite
});
